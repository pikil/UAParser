language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3

jobs:
  include:
    - dist: precise
      php: 5.3
    - dist: trusty
      php: 5.5

before_script:
  - composer install --prefer-source

script:
  - vendor/bin/phpunit --coverage-text
